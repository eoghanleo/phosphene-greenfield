name: "instrument.gantry.prism.global.rework"

on:
  workflow_dispatch: {}

permissions:
  issues: write
  contents: read

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: re-dispatch apparatus with defect context (recirculation)."

