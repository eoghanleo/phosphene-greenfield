name: "instrument.detector.apparatus_log_attach"

on:
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: attach apparatus log artifact to PR as summary/comment."

