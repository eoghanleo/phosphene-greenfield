name: "instrument.condenser.reject"

on:
  workflow_dispatch: {}

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: apply explicit veto/ruling labels and request rework/eject."

