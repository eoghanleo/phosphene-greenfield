name: "instrument.apparatus.modulator.feature-management.feature"

on:
  workflow_dispatch: {}
  workflow_call: {}

permissions:
  issues: write
  contents: read

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: summon modulator for feature specs / FR patterns."

